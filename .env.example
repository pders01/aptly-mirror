APT_DOMAIN=deb.example.com
ACME_EMAIL=admin@example.com

# Space-separated CIDR list. Only these IPs can fetch the mirror.
# ACME http-01 challenge path is always allowed regardless.
# Use 0.0.0.0/0 ::/0 for fully public.
ALLOWED_IPS=192.0.2.10/32 198.51.100.0/24
